{
  "name": "workflow-worker",
  "private": true,
  "scripts": {
    "build": "bun build src/index.ts --target=node --outdir dist --bundle --minify --format=cjs",
    "zip": "rm -f lambda.zip && zip -r lambda.zip dist package.json",
    "build:zip": "bun run build && bun run zip",
    "deploy": "aws lambda update-function-code --function-name n8n-workflow-executor-dev --zip-file fileb://lambda.zip --region ap-south-1",
    "deploy:full": "bun run build:zip && bun run deploy",
    "logs": "aws logs tail /aws/lambda/n8n-workflow-executor-dev --follow --region ap-south-1",
    "build:lambda-zip-windows": "bun run build && Compress-Archive -Path dist,package.json -DestinationPath lambda.zip -Force",
    "test": "vitest",
    "test:run": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@vitest/coverage-v8": "4.1.5",
    "vitest": "^4.1.5"
  },
  "dependencies": {
    "@repo/auth": "*",
    "@repo/db": "*",
    "@types/aws-lambda": "^8.10.161",
    "aws-lambda": "^1.0.7",
    "cron-parser": "^5.5.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}