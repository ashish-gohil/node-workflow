{
  "name": "workflow-worker",
  "private": true,
  "scripts": {
    "build": "bun build src/index.ts --target=node  --outdir dist  --bundle  --minify   --format=cjs"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "dependencies": {
    "@repo/auth": "*",
    "@repo/db": "*",
    "@types/aws-lambda": "^8.10.161",
    "aws-lambda": "^1.0.7"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}