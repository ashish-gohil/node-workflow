{
  "name": "workflow-worker",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun build src/index.ts --target=node  --outdir dist  --bundle  --minify   --format=cjs"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "dependencies": {
    "@repo/auth": "*",
    "@repo/db": "*"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}